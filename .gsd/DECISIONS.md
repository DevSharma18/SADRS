# DECISIONS.md

## Log
- **2026-02-23**: Initialized project structure to track the login hotfix. Defaulted to maintaining the existing `express-session` and `bcrypt` stack instead of rewriting the auth flow.
